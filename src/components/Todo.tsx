import { Button } from '@mui/material'
import React from 'react'

export const Todo = ({ name  }:{name:string}) => {
  return (
    <Button style={{ justifyContent: 'left'}} fullWidth={true} > Item #1</Button>
  )
}
